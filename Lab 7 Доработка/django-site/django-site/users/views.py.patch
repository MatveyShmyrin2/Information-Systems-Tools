# Patch generated by Pyment v0.3.3

--- a/views.py
+++ b/views.py
@@ -9,12 +9,9 @@
 def get_name(request):
     """Функция, получающая данные с формы
 
-    Args:
-      request(Получает реквест с именем пользователя из формы): 
+    :param request: 
+    :type request: Получает реквест с именем пользователя из формы
 
-    Returns:
-
-    
     """
     print('Получен метод запроса: ', request.method)
     if request.method == 'POST':
@@ -34,12 +31,9 @@
 def user_by_name(request):
     """Выводит найденных пользователей
 
-    Args:
-      request(Запрос по имени пользователя): 
+    :param request: 
+    :type request: Запрос по имени пользователя
 
-    Returns:
-
-    
     """
     print('Получили: ', SearchingUsers.ByName)
     substring = '%' + SearchingUsers.ByName + '%'
